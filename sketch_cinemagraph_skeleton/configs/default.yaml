input:
  structural_sketch_path: data/inputs/structural_sketch/smoke.png
  motion_sketch_path: data/inputs/motion_sketch/smoke.png
  prompt_path: data/inputs/prompts/smoke.txt

scene:
  backend: diffusion
  image_size: [768, 512]
  seed: 42
  save_reference: true

  model_id: runwayml/stable-diffusion-v1-5
  controlnet_id: lllyasviel/control_v11p_sd15_scribble
  num_inference_steps: 25
  guidance_scale: 6.5
  controlnet_conditioning_scale: 0.8
  enable_cpu_offload: true
  negative_prompt: "low quality, blurry, distorted, repeated texture, mosaic pattern, extra objects, messy composition"

synthesis:
  num_frames: 60
  fps: 20

output:
  stylized_image_path: data/outputs/scene/stylized_image_smoke.png
  realistic_reference_path: data/outputs/scene/realistic_reference_smoke.png
  debug_dir: data/outputs/debug
  final_video_path: data/outputs/cinemagraphs/output_smoke.gif
